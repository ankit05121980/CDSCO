{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "export NODE_OPTIONS=--max-old-space-size=4096 && pnpm --filter backend build && node -e \"const fs=require('fs');const src=require.resolve('sql.js/dist/sql-wasm.wasm',{paths:['./backend/node_modules','./node_modules']});fs.copyFileSync(src,'./backend/dist/sql-wasm.wasm');console.log('[build] sql.js wasm ->',src)\" && pnpm --filter frontend build",
  "outputDirectory": "dist",
  "functions": {
    "api/[...path].js": {
      "memory": 1024,
      "maxDuration": 60,
      "includeFiles": "backend/dist/**"
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
